extends layout

block content
  section
    h2 Dashboard
    p Welcome to #{user.firstName} #{user.lastName}

    form(action='/users/dashboard' method='post').shadow
          table
            tr
              td
                  textarea(name="text", cols="80", rows="5" placeholder="What is happening?")
            tr
              td
                input(type='submit' value='Yabba')